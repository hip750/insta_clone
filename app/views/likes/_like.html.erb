<% if logged_in? %>
  <% if imagepost.liked_by?(current_user) %>
    <%= link_to imagepost_likes_path(imagepost.id), method: :delete, remote: true do %>
      <span>
        <i class="fa fa-heart unlike-btn" aria-hidden="true"></i>
        <%= imagepost.likes.count %>
      </span>
    <% end %>
  <% else %>
    <%= link_to imagepost_likes_path(imagepost.id), method: :post, remote: true, class: "likes" do %>
      <span>
        <i class="fa fa-heart like-btn" aria-hidden="true"></i>
        <%= imagepost.likes.count %>
      </span>
    <% end %>
  <% end %>
<% else %>
  <span>
    <i class="fa fa-heart unlike-btn" aria-hidden="true"></i>
    <%= imagepost.likes.count %>
  </span>
<% end %>